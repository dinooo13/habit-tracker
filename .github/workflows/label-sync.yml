name: Label sync

# Keeps the repo's labels in sync with .github/labels.yml (the taxonomy documented
# in docs/WORKFLOW.md). Runs on pushes to main that touch the label file, and can be
# triggered manually. Uses the built-in GITHUB_TOKEN — no PAT required.
#
# --allow-added-labels means this only creates/updates labels from the file; it never
# deletes labels that exist on the repo but aren't listed. Drop that flag if you want
# the file to be the single source of truth (extra labels get removed).

on:
  push:
    branches: [main]
    paths: ['.github/labels.yml']
  workflow_dispatch:

permissions:
  contents: read
  issues: write

concurrency:
  group: label-sync
  cancel-in-progress: false

jobs:
  sync:
    name: Sync labels
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 22
      - name: Sync labels from .github/labels.yml
        env:
          GITHUB_ACCESS_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          npx -y github-label-sync@3.0.0 \
            --access-token "$GITHUB_ACCESS_TOKEN" \
            --allow-added-labels \
            --labels .github/labels.yml \
            "$GITHUB_REPOSITORY"
